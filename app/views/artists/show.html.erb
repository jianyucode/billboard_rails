<h1>Artist Info & Songs</h1>


<h2>Name: <%=  @artist.name %></h2>

<ul>

    <% @artist.songs.each do |s| %>
      <li>Title: <%= link_to s.name, artist_songs_path(@artist) %></li>
    <% end %>


</ul>

<%= link_to "Back", artists_path %>
<%= link_to "Add a song", new_artist_song_path(@artist)	 %>
